import {spawn} from 'node:child_process'; import {dirname,join} from 'node:path'; const env={...process.env}; for(const k of Object.keys(env))if(k.toLowerCase()==='path')delete env[k]; env.Path=dirname(process.execPath)+';'+(process.env.Path||process.env.PATH||'').replaceAll('\"',''); const child=spawn(process.execPath,[join(dirname(process.execPath),'node_modules/npm/bin/npm-cli.js'),...process.argv.slice(2)],{env,stdio:'inherit',windowsHide:true});child.on('exit',c=>process.exit(c??1));