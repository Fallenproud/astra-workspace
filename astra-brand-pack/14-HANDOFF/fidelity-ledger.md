# Fidelity ledger

Compared supplied references and browser PNGs at 1672×941; mobile checks at 390×844. Evidence: supplied images in 10-MOCKUPS and rendered captures in the project design/canonical directory.

| Comparison | Evidence / resolution |
|---|---|
| Primary copy | Exact “Give Astra a place to work.” and “Turn ideas into real work.” retained. Workspace preview uses the v1.2 action headline instead of time-dependent greetings. |
| Landing composition | Centered hero, horizontal runtime preview, left navigation and overlapping four-capability strip preserved. Initial preview sat too low; offset and card overlap corrected after screenshot comparison. |
| Typography | Geist is shipped locally. UI controls have deliberate sizes; header/hero weights adjusted against the references. No dependency on a remotely hosted font. |
| Palette / graphics | Removed navy/cyan treatment and diagonal glass streaks. Uses explicit v1.2 colors and low-opacity orbital paths. Light theme follows the supplied light palette. |
| Logo | The v1.0 prism is rejected. Folded-A silhouette reconstructed in SVG from the supplied references; this is not an original vector extraction or a claim of pixel-identical reconstruction. |
| Controls / corners | Silver hover and transparent product buttons preserved; public conversion CTA uses the gradient explicitly permitted by v1.2. Shell corners, borders and composer use the canonical restrained hierarchy. |
| Inspector | Prior user instruction takes precedence over the open screenshot: Home starts without panel/tabs. Show controls reveal actual context and approval state. No fabricated user avatar, quota, upgrade plan or system-ready indicator. |
| Responsive | Fixed decorative horizontal overflow on early access; navigation remains a drawer and inspector a dismissible sheet. Desktop retains the three-pane operational structure. |
| Public claims | “Watch video” becomes “Explore workspace” because no video was supplied. “Sign in” becomes “Open workspace” because this is local vault access. Pricing/docs nav is replaced with the working how-it-works section. Team/enterprise/training-policy claims are replaced with verified local-storage/approval/development facts. |
| Early-access conversion | “Join the waitlist” becomes “Save early-access interest”. Receipt explicitly says local-only; no fake hosted account, email submission or social sign-in. |

Above-the-fold copy differences are intentional functional corrections listed above. No pixel-perfect or independent agency-signoff claim is made. The canonical page hierarchy, identity and operating model are implemented; roadmap limits remain explicit in the acceptance checklist.
